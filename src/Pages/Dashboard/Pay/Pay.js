import React from 'react';
import { Typography } from '@mui/material';
import { blue } from '@mui/material/colors'
const Pay = () => {
    return (
        <div>
            <Typography color={blue[500]} sx={{ mb: 5 }} variant='h3'>Payment Method Coming soon...</Typography>
        </div>
    );
};

export default Pay;